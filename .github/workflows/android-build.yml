name: Build Android APK (Native Ready v2.6)

on:
  push:
    branches: [ "main", "master" ]
  workflow_dispatch:

jobs:
  build:
    name: Generate APK
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'

      - name: Install Dependencies
        run: npm install --legacy-peer-deps --force

      - name: Build Web Application
        run: npm run build

      - name: Setup Java JDK
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'
          cache: 'gradle'

      - name: Ultimate Definitive Fix - Core Plugin and Platform Resolution (GBR Protocol v2.6)
        run: |
          # 1. Ajusta os plugins e remove versões defeituosas
          npm uninstall @capacitor/device
          npm install @capacitor/device@6.0.2 --save-exact
          
          # 2. Re-injeta explicitamente o core do Android do Capacitor para garantir o mapeamento do CLI
          npm install @capacitor/android@6.2.1 --save-exact
          
          # 3. Força a criação ou link da plataforma nativa caso ela tenha sido desvinculada
          npx cap add android || true
          
          # 4. Sincroniza os ativos e metadados finais
          npx cap sync android

      - name: Build Debug APK
        run: |
          cd android
          chmod +x gradlew
          ./gradlew assembleDebug --no-build-cache --no-daemon --stacktrace

      - name: Upload Android Debug APK
        uses: actions/upload-artifact@v4
        with:
          name: gbr-inventario-debug-apk
          path: android/app/build/outputs/apk/debug/app-debug.apk
          retention-days: 7
